# QA backlog (post–Day-3 sweep)

Add rows as you find issues during **final QA** and **post-launch monitoring**. Non-blocking items stay here until prioritized.

| ID | Area | Description | Severity | Status |
|----|------|-------------|----------|--------|
| QA-001 | Build | Turbopack / Sentry: `import-in-the-middle` version mismatch warning during `next build` | Low | Open |
| QA-002 | Platform | Next.js deprecation: `middleware` → `proxy` migration | Low | Open |

### How to use

1. After the production QA sweep, append new rows (increment ID).
2. “Blocking launch” bugs should be fixed immediately in code, not listed here.
3. Burn down this list during the week after Webflow sunset.
