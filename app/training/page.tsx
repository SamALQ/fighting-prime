import { TrainingClient } from "./training-client";

export const metadata = {
  title: "Training",
  description: "Drill timer, training log, and challenges",
};

export default function TrainingPage() {
  return <TrainingClient />;
}
